{
  "name": "cms-0057-f",
  "private": true,
  "description": "Static educational site for cms-0057-f.com",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test scripts/worker.test.mjs",
    "build": "npm run validate && npm test",
    "preview": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "verify:https": "node scripts/verify-https.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
